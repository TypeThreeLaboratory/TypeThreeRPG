package marumasa.type_three_rpg.entity.player;

import marumasa.type_three_rpg.minecraft;
import org.bukkit.entity.Player;

public class mainPlayer {
    public static void setSkillSlot(Player player, minecraft mc) {


        /*final ItemStack itemStack = new ItemStack(Material.STRUCTURE_VOID);
        final List<Component> list = new ArrayList<>(List.of(Component.text("§fこのスロットは移動できません")));
        ItemMeta itemMeta = itemStack.getItemMeta();
        itemMeta.displayName(Component.text("スキル: なし"));
        itemMeta.lore(list);
        itemStack.setAmount(1);
        itemStack.setItemMeta(itemMeta);


        final Inventory inventory = player.getInventory();

        if (Objects.equals(Meta.get(player, mc, "role", ""), "")) {
            Bukkit.getLogger().info("rgferh");
            inventory.setItem(6, itemStack);
        }
        if (Meta.get(player, mc, "level", 1) < 20) {
            Bukkit.getLogger().info("ewr");
            inventory.setItem(7, itemStack);
        }
        if (Meta.get(player, mc, "level", 1) < 40) {
            Bukkit.getLogger().info("er");
            inventory.setItem(8, itemStack);
        }*/
    }
}
